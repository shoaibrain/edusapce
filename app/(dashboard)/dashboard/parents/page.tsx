import { FC } from 'react'

interface pageProps {
  
}

const page: FC<pageProps> = ({}) => {
  return <div>
   <h1>Parenta Account module</h1>
   <p>Module that simulates parent account ledger</p>
  </div>
}

export default page